export class Kitten {
  name: string;
  race: string;
  birth: string;
  pictureUrl: string;

  constructor(name: string, race: string, birth: string, pictureUrl: string) {
    this.name = name;
    this.race = race;
    this.birth = birth;
    this.pictureUrl = pictureUrl;
  }
}
